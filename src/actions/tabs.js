import { CHANGE_SELECTED_TAB } from '../constants/ActionTypes';

export const changeSelectedTab = (index) => ({ type: CHANGE_SELECTED_TAB, index }); // eslint-disable-line import/prefer-default-export
